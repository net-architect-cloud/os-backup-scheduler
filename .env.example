# OpenStack Automatic Backup - Environment Variables
# Copy this file to .env and fill in your values for local testing
# For GitHub Actions, configure these as Secrets and Variables in your repository settings

# ===================
# GitHub Secrets (sensitive)
# ===================
OS_USERNAME=your-openstack-username
OS_PASSWORD=your-openstack-password
OS_PROJECT_NAME=your-project-name

# ===================
# GitHub Variables (non-sensitive)
# ===================
OS_AUTH_URL=https://api.example.cloud/identity
OS_USER_DOMAIN_NAME=Default
OS_PROJECT_DOMAIN_NAME=default
OS_IDENTITY_API_VERSION=3
OS_REGION_NAME=region-a

# ===================
# Optional
# ===================
RETENTION_DAYS=14

# Use snapshot method for volume backups (avoids --force which can get stuck)
# Set to "true" to use snapshot -> volume -> backup method for attached volumes
# Set to "false" to use direct backup with --force
USE_SNAPSHOT_METHOD=true

# Timeout for waiting on resources during snapshot method (in seconds)
RESOURCE_TIMEOUT=1800
